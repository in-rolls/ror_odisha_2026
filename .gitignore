# The crawl output is large and re-derivable: every khatiyan checkpoint can be
# refetched, and the parsed table is rebuilt by parse_ror.py.
/raw

# Crawl telemetry. Only failures are recorded, but a full-state run still
# produces tens of MB and none of it is worth versioning.
logs/

__pycache__/
*.py[cod]
.venv/
venv*/
.DS_Store
.pytest_cache/
*.egg-info/
